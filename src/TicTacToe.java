import java.awt.*;
import java.awt.event.*;
import javax.swing.*;

public class TicTacToe {
    int boardWidth = 600;
    int boardHeight = 650; // 50 px for the text pannel on top

    JFrame frame = new JFRame();

    TicTacToe() {
        frame.setVisible(true);
        frame.setSize(boardWidth, boardHeight)
    }
}
